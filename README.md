# 🎧 Taglist

**Taglist** — музыкальный плеер, в котором _плейлисты — это шаблоны тегов_.  
Можно создавать собственные подборки по жанрам, настроению и любым кастомным тегам.

Проект написан на **React + TypeScript** с архитектурой **FSD (Feature-Slice Design)**.  
Интерфейс — полностью адаптивный, а состояние управляется через **Zustand**.  
Анимации — на **Motion**, стили — **SCSS**, сборка — **Vite**.

👉 [Посмотреть демо](https://taglist-remake.vercel.app/)  
Если вы в РФ — включите VPN, иначе данные не подгрузятся.

---

## 🚀 Стек

**Frontend**

- ⚛️ React 19 + TypeScript
- 🎛 Zustand — лёгкий state-менеджер
- 🎨 SCSS — со строгим stylelint-контролем
- 🌀 Motion — анимации
- 🌍 i18next — мультиязычность
- ⚙️ Vite — сборка и hot reload

**Code Quality**

- 🧹 ESLint + TypeScript-ESLint
- 💅 Prettier
- 🔍 Stylelint + @stylistic

---

## 🧩 Установка и запуск

```bash
# установить зависимости
npm ci

# запустить dev-режим
npm run dev

# билд для продакшена
npm run build

# форматирование кода
npm run format

# линтеры
npm run lint
npm run lint:eslint:fix
npm run lint:stylelint:fix
```
